# Web application

Reserved for the factory-operator UI. The first UI milestone will consume the
versioned API and display sensor status, recent readings, and active alerts.

The API contract and operational evidence take priority over visual polish in
the first 30-day iteration.
