# Analytics service

Future extraction boundary for aggregations, anomaly candidates, and feature
inputs. The initial implementation may live inside `apps/api` so that the
domain behaviour can be tested before introducing a network boundary.
