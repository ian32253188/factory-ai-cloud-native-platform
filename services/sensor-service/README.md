# Sensor service

Future extraction boundary for sensor registration and reading ingestion. It
must not be treated as an independently running service until its contract,
idempotency behaviour, health checks, and ownership are implemented and tested.
