# ML inference service

Future versioned inference API. Model artifacts, private datasets, and secrets
must stay outside Git; the repository should retain only reproducible code,
metadata, safe fixtures, and verification evidence.
