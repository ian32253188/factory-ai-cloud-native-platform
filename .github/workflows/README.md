# CI/CD workflows

The first workflow will run formatting/lint checks and the test suite on pull
requests and pushes to `develop`. Build, image scanning, deployment, and GitOps
promotion workflows will be added as their corresponding milestones become
executable.
